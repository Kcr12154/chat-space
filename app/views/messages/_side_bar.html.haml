.side-bar
  .side-contents
    .side-top
      .top-left
        = current_user.name
      .top-right
        .icon-left
          = link_to"#" do
            =icon('fas','edit')
        .icon-right 
          = link_to edit_user_path(current_user) do
            =icon('fas', 'cog', class: 'icon')
  .groups
    .side-over
      teccamp
    .side-under
      まだメッセージがありません

      -# = current_user.name
      -#         = link_to edit_user_path(current_user) do
      -#     = icon('fas', 'cog', class: 'icon')